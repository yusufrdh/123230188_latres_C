name: latres_123230188_c
description: "Proyek Latihan Responsi IF-C - Toko Online Premium"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

  # Library yang diwajibkan dalam soal 
  http: ^1.2.1
  shared_preferences: ^2.2.3
  hive: ^2.2.3
  hive_flutter: ^1.1.0

  # Icons (Opsional, untuk mendukung tampilan clean)
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  # Linter untuk memastikan kode tetap rapi dan profesional
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true

  # Jika Anda memiliki aset gambar lokal, aktifkan di sini:
  # assets:
  #   - assets/images/